# 模块渲染 A/B 实验记录

> 每轮只改变 `render_profile`。若同时改变模型、素材、自然语言意图、模块组合、seed、采样参数或视频时长，本轮只能作为观察，不能作为严格 A/B。

## 1. 实验身份

- 实验 ID：
- 日期：
- 测试者：
- fixture ID（优先使用 `templates/h3_regression_cases.json`）：
- 本轮假设：

## 2. 固定条件

- H3 模型/权重：
- 提示词模型：
- provider：
- wire / API 路径类型：
- 上下文上限：
- 任务类型：
- 自然语言意图（全文或内容哈希）：
- 素材清单（只写角色，不写私人绝对路径）：
- 素材/媒体 manifest 哈希：
- 创作规则组合：
- 时长 / 分辨率 / FPS / steps：
- seed 或成对 seed：
- 其它必须固定的节点参数：

## 3. 三档输入证据

| 档位 | module_set_sha256 | rendered_system_sha256 | request_sha256 | Prompt IR 输出哈希 | 输出文件名 |
|---|---|---|---|---|---|
| standard |  |  |  |  |  |
| compact |  |  |  |  |  |
| strong |  |  |  |  |  |

这些值从“规则解析清单”和导演报告复制。不要粘贴 API Key、Authorization header、完整本地路径或供应商原始错误正文。

## 4. 结果检查

每项使用 `通过 / 部分通过 / 失败 / 不适用`，并写一条可复核证据。

| 检查项 | standard | compact | strong |
|---|---|---|---|
| 用户核心意图 |  |  |  |
| 主体/产品身份 |  |  |  |
| 动作与接触顺序 |  |  |  |
| 运镜与时间结构 |  |  |  |
| 背景/构图/灯光保留 |  |  |  |
| 可见文字/对白逐字性 |  |  |  |
| 声音策略 |  |  |  |
| 是否出现规则外扩写 |  |  |  |
| 是否出现重绘/续写误判 |  |  |  |

## 5. 归因审查

- 三档是否使用完全相同的输入素材：
- 三档是否使用相同模型、provider 与 wire：
- 三档是否只改变 `render_profile`：
- 是否发生网络重试、JSON 修复或 Prompt IR 修复：
- 是否存在随机性足以解释差异：
- 是否需要用第二组 seed 重复：

## 6. 结论等级

- [ ] 单次观察：只记录现象，不修改通用规则。
- [ ] 同一 fixture 多 seed 重复：可形成局部假设。
- [ ] 多个 fixture 重复同一失败：可修改通用契约或渲染逻辑。
- [ ] 通用 strong 仍稳定失败，且只在同一模型/provider/wire 出现：才评估窄范围 measured override。

结论：

建议改动：

不应改动：
